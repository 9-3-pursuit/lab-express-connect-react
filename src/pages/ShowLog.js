import LogDetails from "../components/LogDetails";

const ShowLog = () => {
    return (
        <div className="Show">
            <h2>Log</h2>
            <LogDetails/>
        </div>
    )
};

export default ShowLog;